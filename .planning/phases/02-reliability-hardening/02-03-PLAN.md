---
phase: 02-reliability-hardening
plan: "03"
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - run_campaign.py
autonomous: true
requirements:
  - ORCH-01
must_haves:
  truths:
    - "run_campaign.py coordinates all steps from killing browser locks to running the scraping, copywriting, dry-run, and campaign sending."
    - "run_campaign.py validates the Facebook session state and halts early if login is required."
  artifacts:
    - run_campaign.py
    - tests/test_campaign.py
  key_links:
    - "run_campaign.py invokes scrape_fb_info.py, generate_copy_llm.py, and send_outreach.py sequentially."
---

<objective>
Implement a master campaign orchestration script run_campaign.py.

Purpose: Provide a unified, robust CLI interface that coordinates all steps of the outreach pipeline sequentially (browser lock releasing, Facebook session verification, scraping, copywriting generation, dry-run validation, and adaptive-delay outreach sending). This eliminates operator errors and allows autonomous agents (like Hermes) to manage campaigns safely with a single command.
Output: run_campaign.py master orchestrator, and tests/test_campaign.py unit tests.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-reliability-hardening/02-CONTEXT.md
@.planning/phases/02-reliability-hardening/02-AI-SPEC.md
@.planning/phases/02-reliability-hardening/02-RESEARCH.md
@.planning/phases/02-reliability-hardening/02-VALIDATION.md
@setup_session.py
@scrape_fb_info.py
@generate_copy_llm.py
@send_outreach.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Master Campaign Orchestrator</name>
  <files>run_campaign.py</files>
  <read_first>
    - run_taichung_20.py
    - setup_session.py
    - send_outreach.py
  </read_first>
  <action>
    Create a new Python script `run_campaign.py` that implements a sequential orchestration flow:
    1. **Kill lingering profiles:** Run subprocess `pkill -f browser_profile` (or target browser paths) to release any profile locks.
    2. **Session Verification:** Check if the user's CloakBrowser profile contains valid cookies for Facebook/Messenger (importing browser context check or checking session cookies).
    3. **Scraping Step:** Call `scrape_fb_info.py` via subprocess.
    4. **Copy Generation Step:** Call `generate_copy_llm.py` via subprocess.
    5. **Dry-Run Verification:** Call `send_outreach.py --dry-run --limit 2` to generate validation screenshots.
    6. **Outreach Campaign:** Call `send_outreach.py` with custom parameters (limit, delay-min, delay-max) passed via `run_campaign.py` CLI arguments.
  </action>
  <verify>
    <automated>python3 -c "import run_campaign; print('Success')"</automated>
  </verify>
  <done>
    run_campaign.py is implemented, supporting parameters for limit and delay bounds, and executes the pipeline sequentially.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Session Check Gate and CLI Argument Routing</name>
  <files>run_campaign.py</files>
  <read_first>
    - run_campaign.py
    - setup_session.py
  </read_first>
  <action>
    Harden the session gate inside `run_campaign.py`:
    1. Implement a quick headless check to verify that `facebook.com` and `messenger.com` load in logged-in states (no "login_required" markers in the page).
    2. If the session is invalid, print a prominent CLI error: "❌ Facebook session expired. Please run 'python3 setup_session.py' to re-authenticate." and exit.
    3. Set up `argparse` to accept `--limit`, `--delay-min`, `--delay-max`, and `--dry-run-only` to control the downstream subprocess invocations.
  </action>
  <verify>
    <automated>python3 run_campaign.py --help</automated>
  </verify>
  <done>
    The script parses arguments correctly and stops early with a descriptive warning if the browser profile is unauthenticated.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement Unit and Mock Tests for Campaign Orchestrator</name>
  <files>tests/test_campaign.py</files>
  <read_first>
    - run_campaign.py
  </read_first>
  <action>
    Create `tests/test_campaign.py` verifying the orchestration logic:
    1. Mock subprocess calls to ensure they are invoked in the correct order with correct CLI flags.
    2. Mock valid/invalid session states and assert that the script terminates early on expired sessions.
    3. Assert that argument routing (`--limit`, `--delay-min`, etc.) maps correctly to subprocess calls.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_campaign.py</automated>
  </verify>
  <done>
    Orchestrator unit tests pass, validating sequential execution, session checking, and parameter parsing.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI arguments → run_campaign.py | Input arguments are parsed and sanitized before running shell subprocesses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Elevation of Privilege | run_campaign.py | mitigate | Limit subprocess execution to hardcoded local Python script paths; do not pass raw string shell inputs to subprocess. |
</threat_model>

<verification>
Run `python3 -m pytest tests/test_campaign.py` to verify sequential execution and session validation routing.
</verification>

<success_criteria>
- Unit test suite `tests/test_campaign.py` passes.
- Running `run_campaign.py` with an invalid session exits immediately with a login reminder.
- Running `run_campaign.py` with a valid session successfully executes the full sequence.
</success_criteria>

<output>
After completion, create `.planning/phases/02-reliability-hardening/02-03-SUMMARY.md`
</output>
