---
phase: 02-reliability-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - generate_copy_llm.py
autonomous: true
requirements:
  - SCRP-05
  - COPY-04
must_haves:
  truths:
    - "Clinics with empty Intro and Latest_Post are assigned the generic fallback copy in the CSV without calling the LLM."
    - "If the LLM output fails Pydantic validation or is malformed JSON, the script retries and eventually falls back to the generic copy."
  artifacts:
    - generate_copy_llm.py
    - tests/test_fallback.py
  key_links:
    - "generate_copy_llm.py reads clinics西醫.csv and writes fallback copies back to it."
---

<objective>
Implement metadata validation, Pydantic schema validation, and fallback copywriting logic inside generate_copy_llm.py. 

Purpose: Prevent silent skipping of clinics that lack Facebook Intro or Latest Post metadata. Instead of skipping them or producing empty output, automatically route them to the pre-approved generic outreach copy template. Ensure all generated copies conform to Traditional Chinese, regulatory blacklists, and length constraints.
Output: Hardened generate_copy_llm.py, and tests/test_fallback.py unit test suite.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-reliability-hardening/02-CONTEXT.md
@.planning/phases/02-reliability-hardening/02-AI-SPEC.md
@generate_copy_llm.py
@generate_generic_copies.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Pydantic Validation Schema & Fallback Copy Definition</name>
  <files>generate_copy_llm.py</files>
  <read_first>
    - generate_copy_llm.py
    - generate_generic_copies.py
    - .planning/phases/02-reliability-hardening/02-AI-SPEC.md
  </read_first>
  <action>
    Define the pre-approved B2B fallback copywriting text as `GENERIC_COPY` inside `generate_copy_llm.py` (copying it from generate_generic_copies.py). Install and import pydantic. Define `ClinicCopySchema(BaseModel)` with a field `personalized_copy` and a `field_validator` verifying:
    1. Length is between 80 and 180 characters.
    2. Character set uses Traditional Chinese (no simplified character leakage).
    3. Excludes Medical Care Act blacklisted superlatives ("最佳", "最先進", "保證療效", "根治", "全台第一").
  </action>
  <verify>
    <automated>python3 -c "import pydantic; from generate_copy_llm import ClinicCopySchema; print('Success')"</automated>
  </verify>
  <done>
    ClinicCopySchema is imported without error and correctly validates valid copies while raising ValidationError on invalid/simplified/blacklisted copies.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Row Processing to Route and Validate Fallbacks</name>
  <files>generate_copy_llm.py</files>
  <read_first>
    - generate_copy_llm.py
  </read_first>
  <action>
    Modify `main()` and helper routines in `generate_copy_llm.py` to check `Intro` and `Latest_Post` values for each row:
    1. If both fields are empty, write the `GENERIC_COPY` immediately to `Personalized_Copy` and log the routing (no LLM API call).
    2. If fields exist, run the local LLM call. Parse the output JSON, validate against `ClinicCopySchema`. If validation fails, retry up to 2 times. If retries are exhausted, log the failure and write `GENERIC_COPY` to ensure completion.
  </action>
  <verify>
    <automated>python3 -c "import generate_copy_llm; print('Success')"</automated>
  </verify>
  <done>
    The script processes clinics with missing metadata by assigning them the generic copy and validating LLM-generated copies, saving results back to clinics西醫.csv.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement Unit Tests for Fallback Routing and Validation</name>
  <files>tests/test_fallback.py</files>
  <read_first>
    - generate_copy_llm.py
  </read_first>
  <action>
    Create `tests/test_fallback.py` to verify:
    1. `ClinicCopySchema` correctly accepts compliant Traditional Chinese copies.
    2. `ClinicCopySchema` raises validation errors on simplified Chinese, blacklist terms, or out-of-bound character lengths.
    3. Mocking `call_local_llm` to return invalid/valid output and verifying that `generate_with_retry` correctly applies retries and falls back to `GENERIC_COPY` on failure.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_fallback.py</automated>
  </verify>
  <done>
    Unit tests pass completely, verifying the fallback and validation logic.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM API → generate_copy_llm.py | Untrusted content from local LLM is validated before writing to CSV |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Personalized_Copy | mitigate | Validate Traditional Chinese and regulatory superlatives using Pydantic validators before writing to the database. |
</threat_model>

<verification>
Run `python3 -m pytest tests/test_fallback.py` to verify validation constraints and fallback logic.
</verification>

<success_criteria>
- Unit test suite `tests/test_fallback.py` passes.
- Running `generate_copy_llm.py` assigns `GENERIC_COPY` to clinics missing both FB Intro and Latest Post.
- LLM outputs violating character length or blacklist constraints are automatically corrected via retry/fallback.
</success_criteria>

<output>
After completion, create `.planning/phases/02-reliability-hardening/02-01-SUMMARY.md`
</output>
