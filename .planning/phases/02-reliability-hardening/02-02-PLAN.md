---
phase: 02-reliability-hardening
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - send_outreach.py
autonomous: true
requirements:
  - SEND-05
  - ERR-04
must_haves:
  truths:
    - "If a warning signal (無法傳送 or 暫時限制) is found on the page, the delay multiplier is doubled for subsequent messages."
    - "If the consecutive warning or block count exceeds 3, the script automatically aborts execution, exits, and registers a hard warning."
  artifacts:
    - send_outreach.py
    - tests/test_backoff.py
  key_links:
    - "send_outreach.py reads Personalized_Copy and outcomes are logged in outreach_sent_log.jsonl"
---

<objective>
Implement adaptive delay backoff and error circuit-breaker logic inside send_outreach.py.

Purpose: Protect the Facebook outreach account from getting permanently banned when Facebook rate limits or soft blocks are triggered. Double random delays on warning signals, and halt execution automatically before account damage occurs.
Output: Hardened send_outreach.py, and tests/test_backoff.py unit test suite.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-reliability-hardening/02-CONTEXT.md
@.planning/phases/02-reliability-hardening/02-AI-SPEC.md
@send_outreach.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Soft Block Detection and Adaptive Delay Backoff</name>
  <files>send_outreach.py</files>
  <read_first>
    - send_outreach.py
  </read_first>
  <action>
    Modify `send_outreach.py` to trace a dynamic `delay_multiplier` (initially 1.0).
    In the browser page checking logic, scan the DOM page source and warning banners for Facebook rate-limit indicators:
    1. If the warning keywords "無法傳送" or "暫時限制" are detected:
       - Double `delay_multiplier` (e.g. multiplier *= 2.0).
       - Adjust the random sleep logic for the next iteration to multiply limits (e.g., `random.uniform(delay_min * delay_multiplier, delay_max * delay_multiplier)`).
       - Log the backoff event to outreach_sent_log.jsonl under `status="backoff"`.
    2. If a message is successfully sent, slowly scale the multiplier back down (e.g. max(1.0, delay_multiplier * 0.9)) to recover normal sending speed.
  </action>
  <verify>
    <automated>python3 -c "import send_outreach; print('Success')"</automated>
  </verify>
  <done>
    Adaptive delay backoff is integrated, scaling delay boundaries dynamically on soft blocks.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Session Warning Threshold and Hard Circuit Breaker</name>
  <files>send_outreach.py</files>
  <read_first>
    - send_outreach.py
  </read_first>
  <action>
    Add a warning counter `warning_count` tracking consecutive blocks or rate limit events in the current session.
    1. If a warning is encountered, increment `warning_count`.
    2. If a message is successfully sent, reset `warning_count` to 0.
    3. If `warning_count >= 3`, log the threshold breach to `outreach_sent_log.jsonl` under `status="session_halted"`, print a prominent CLI alert, and immediately exit the process (`sys.exit(1)`) to shut down the campaign safely.
  </action>
  <verify>
    <automated>python3 -c "import send_outreach; print('Success')"</automated>
  </verify>
  <done>
    The script terminates automatically and prints a critical CLI alert when 3 consecutive soft blocks occur.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement Unit Tests for Backoff and Circuit Breaker</name>
  <files>tests/test_backoff.py</files>
  <read_first>
    - send_outreach.py
  </read_first>
  <action>
    Create `tests/test_backoff.py` containing mocked browser responses and sending states:
    1. Mock browser checks returning warning/block signals and verify that `delay_multiplier` scales up correctly.
    2. Verify that consecutive blocks increment `warning_count` and trigger `sys.exit(1)` upon reaching the limit of 3.
    3. Verify that a successful send resets the warning count and decreases the multiplier toward 1.0.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_backoff.py</automated>
  </verify>
  <done>
    Unit tests pass completely, verifying backoff scaling and circuit breaker safety logic.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Facebook UI → send_outreach.py | Rate limits and block indicator strings are detected in the untrusted Facebook UI DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Denial of Service | send_outreach.py | mitigate | Monitor consecutive failures and halt the process immediately on 3 blocks to prevent permanent account ban. |
</threat_model>

<verification>
Run `python3 -m pytest tests/test_backoff.py` to verify backoff and circuit-breaker behavior under mock warnings.
</verification>

<success_criteria>
- Unit test suite `tests/test_backoff.py` passes.
- Mock warnings trigger exponential scaling of delays.
- Consecutive warning counts of 3 trigger a hard script exit.
</success_criteria>

<output>
After completion, create `.planning/phases/02-reliability-hardening/02-02-SUMMARY.md`
</output>
